| # | FIXED / PARTIAL / NOT FIXED | file:line evidence | Does a test actually fail without the fix? |
|---|---|---|---|
| 1 | **PARTIAL** | `xr/rank.py:113,120–122` strips `ANTHROPIC_BETAS` and supplies `--tools "" --strict-mcp-config`. However, inspecting installed Claude 2.1.280’s embedded source shows `--json-schema` subsequently appends the built-in, read-only `StructuredOutput` tool. The literal **zero built-in tools** requirement remains unmet; MCP isolation is not live-tested. | **Yes:** `tests/test_research.py:183` fails against baseline because `--strict-mcp-config` is absent. It checks argv/env, not the actual exposed tool list, so misses `StructuredOutput`. |
| 2 | **FIXED** | `xr/rank.py:103–104` appends six SHA-1 hex characters derived from sorted evidence URLs. | **Yes:** `tests/test_research.py:195` fails against baseline because colliding slugs produce identical IDs; fixed code passes and preserves order independence. |
| 3 | **FIXED** | `xr/store.py:110–117` collects evidence URLs from topics used on the requested platform and excludes overlapping candidates. | **Yes:** removing the overlap filter makes `tests/test_research.py:207` fail on the returned topics. Other-platform availability also passes. |
| 4 | **PARTIAL** | `xr/store.py:59–70` handles malformed IPv6 URLs and reports them through `xr/research.py:77–79`. The guard is incomplete: `float(signal)` validates without converting; duplicate items with signals `"10"` and `2` still raise `TypeError` at `xr/store.py:73`, outside the guard. | **Yes:** `tests/test_research.py:218` passes fixed code; baseline raises `ValueError: Invalid IPv6 URL`. The mixed-signal case remains uncovered. |
| 5 | **PARTIAL** | `xr/sources.py:142–175` handles missing/short envelopes and preserves returned successes. But `{"results":[{"successful":true}]}` yields no items **and no errors**; malformed `data` can abort the whole batch at `:223`. A subprocess timeout also escapes without per-call errors. | **Yes:** tests at `tests/test_research.py:238,247,265` fail against baseline. The mixed-success test at `:255` fails baseline first on missing `check=False`, not on lost successes. |
| 6 | **FIXED** | `xr/sources.py:121–137,173–175` reports unreadable/undecodable spill files and attributes failure to the affected call while retaining other calls. | **Yes:** `tests/test_research.py:276` passes with filesystem operations mocked; baseline raises `JSONDecodeError` and loses the successful sibling result. Native execution is blocked by sandbox temp-file restrictions. |
| 7 | **PARTIAL** | `xr/sources.py:63–66,87–93` isolates transport/JSON-decoding failures, but not all parsing failures. Reproduced: a successful first query followed by `{"hits":null}` or `{"items":null}` makes the source return count zero, losing the first query’s items. GitHub parsing remains outside its `try`. | **Yes:** tests at `tests/test_research.py:291,300` fail against baseline on the injected network/CLI exceptions. Neither tests malformed response shapes. |
| 8 | **FIXED** | `xr/research.py:105–111,129–133` preserves existing topics after a caught rank failure, writes `research.failed.json`, and uses temporary files plus `os.replace`. Additional regressions are below. | **Yes, for preservation:** `tests/test_research.py:312` fails against baseline and passes with filesystem operations mocked. **No atomicity coverage:** it also passes when `_atomic_write` is replaced with direct writes. |
| 9 | **FIXED** | `xr/sources.py:228–235` drops trailing partial buckets. Fixture result verified as `last=28.0`, `mean12=46.3`, `momentum=0.6`. | **Yes:** `tests/test_research.py:335` fails against baseline and passes fixed code. |
| 10 | **FIXED** | `tests/test_research.py:183,238–288` now exercises `claude_json`, `run_composio`, and spill handling. | **Yes:** relevant baseline/mutation checks fail as described above. Offline native run: **24 pass, 5 blocked by temp-file restrictions**; with filesystem operations mocked: **29 pass**. |

| Severity | file:line | NEW defect or regression introduced by the diff; evidence |
|---|---|---|
| **Major** | `xr/research.py:105–108` | **Failed reruns discard newly collected evidence.** Reproduced successful run with item A, then failed ranking with new item B: none of the four output files contains B or its snippet; `research.failed.json` contains only counts/errors. SQLite retains limited metadata, not snippet/published details (`xr/store.py:43–44,84`). Baseline preserved the failed run’s items in `items.json`. |
| **Major** | `xr/research.py:87` | **Narrowed exception handling can abort before saving collected items.** With mocked Claude stdout `[]`, the real rank path raises `AttributeError` at `xr/rank.py:126`. Baseline catches it and writes items/errors; fixed code aborts without reaching the writer. Reproduced offline. |
| **Major** | `xr/research.py:131–133` | **Concurrent writers share the same temporary filename.** Deterministically interleaving two `_atomic_write` calls makes one replace the other’s temporary file, then the other raises `FileNotFoundError`. Overlapping runs therefore lack independent staging and can publish another run’s contents. The added test does not exercise concurrency. |
| **Minor** | `pyproject.toml:5–12` | **The required Ruff version pin is absent.** Python target and line length are configured, and `ruff check --no-cache xr tests` passes, but no Ruff dependency/version constraint makes the lint gate reproducible as specified. |